/// <reference types="vite/client" />

// Injected by Vite `define` at build time (see vite.config.ts). Used for the
// footer copyright year so server-prerendered HTML and client hydration agree.
declare const __BUILD_YEAR__: number;
