# PROGRESS — archi-made.com. Branch seo/finalize (off seo/phase-1-indexability). LOCAL ONLY. [ ]todo [x]done [~]partial

## DECISION (2026-06-19) — Brand = "ArchiMade Studio" (final) + DEPLOY-READY

"Studio" is legally safe (only "architecte/architecture" mattered → stays 0). The earlier "drop Studio" was a misstep: reverted the preloader edit (splash keeps "ARCHI MADE STUDIO"); restored "Studio" in the 4 route titles (reverses cff0596). Brand now consistent: 4 titles + og:site_name + footers + aria-labels all "ArchiMade Studio"; Instagram handle archi.made.studio untouched (immutable). FINAL cert: build green 4 routes (max chunk 307 KB), seo-check 25/25, verify-image 0 fail; architecte/architecture = 0; block B intact (H1 + 6/6 service H2s + title/meta); JSON-LD ProfessionalService (no Architect) + legalName ARCHI-MADE LTD + PostalAddress Tours + areaServed + 7 services + no aggregateRating; mentions légales complete; OG 1200×630 + w/h meta; NAP consistent. **DEPLOY-READY: YES** (code-complete; remaining = live-only per deploy-runbook.md + Fayçal text validation + GBP). Runbook gap: §1 still names old branch `seo/phase-1-indexability` (operator should use `seo/finalize`).

## DECISION (2026-06-19) — Architect pivot ABANDONED

Official RNE/INPI lookup: APE **7112B** "Ingénierie, études techniques", déclared object **"Conception de plans dans le domaine du bâtiment"** → the client is **NOT an architect**. The `seo/architect-pivot` branch (which flipped to `@type Architect` + "architecte" keyword) is dead — NOT merged. `seo/finalize` branches off the certified non-architect HEAD `3a99891` and finalizes the legally-safe version. Confirmed identity now locked in CLAUDE.md Business (legalName ARCHI-MADE LTD, brand ArchiMade, SIREN 101 715 993, SIRET 10171599300024, APE 7112B, établissement 25 rue du Maréchal Ney 37100 Tours, siège London, areaServed Indre-et-Loire/Tours + France). seo-check guard correct: `@type "Architect"` present = FAIL.

## P1 Indexability

[x] prerender 4 routes [x] per-route head [x] robots.txt [x] sitemap.xml
[x] canonical www [x] console clean [x] hydration(**BUILD_YEAR**)
[x] .npmrc legacy-peer-deps [x] soft-404 → 404/noindex

## P2 Structured data & social

[x] JSON-LD in raw HTML [x] Organization(@id,logo,sameAs,contactPoint)
[x] ProfessionalService (NOT Architect; areaServed=Indre-et-Loire+Tours+France) [x] Service nodes→@id
[x] no self-rating [x] real 1200x630 OG card

## P0 Design regression

[x] header logo restored to original size (h-44 md:h-56 = 176/224px, scale-100, no CSS upscale)

## P3 Performance/CWV

[x] WebP (3D PNGs 2.5–3MB → <300KB) [x] width/height every img [x] hero eager+fetchpriority / kill below-fold preload / lazy rest
[x] srcset/sizes + AVIF `<picture>` [x] code-split <500KB/chunk [x] fonts preconnect+preload+swap
[~] tasks <50ms — field CWV only (deploy-day PSI/CrUX)

## P3 Image delivery — GSAP/CSS gap closure [x]

[x] preloader-content exit: blur only (no scale > 1)
[x] gallery lightbox: opacity-only motion (no scale 0.9→1)
[x] project modal hero: gsap.set + opacity-0 before tween
[x] expertise expanded: sizesScale 1.2 on service ResponsiveImage
[x] full-bleed [image-rendering:auto] on methodes / values / modal hero
[x] audit-image-transforms.mjs permanent + verify integration
[x] remaster-images.mjs restored + npm run remaster-images
[x] audit 0 fail · verify 0 fail (default) · seo-check 25/25

[~] PNG originals restore → npm run remaster-images → verify --strict-sources (deploy-day assets; public/IMAGES/ not on disk)

## P4 Measure & security

[x] GA4 consent-gated (src/lib/gtag.ts, VITE_GA_ID) [x] GSC verification (VITE_GSC_ID meta)
[x] rate-limit /api/send-email [x] honeypot + time-trap + escape email + remove body log

## Audit vs Standards 2026 (2026-06-18)

- P1 Indexability: 8/8 (100%)
- P2 Structured Data: 6/6 (100%) + areaServed SET (Indre-et-Loire + Tours + France)
- P3 Performance/CWV: 11/12 (92%) — tasks<50ms PARTIAL (field data)
- P4 Security: 6/6 (100%)
- **Total: 32/32 static (100%)** · seo-check **25/25 PASS** · verify-image **0 fail** (tasks<50ms = deploy-day field metric only)

## DECISIONS / BLOCKED

- Market: DECIDED — local base (Indre-et-Loire/Tours) + national remote reach. areaServed set in StructuredData.tsx (AdministrativeArea Indre-et-Loire + City Tours + Country France). Geo copy left as-is (no invented city in visible content).

## Deploy-day MANUAL checklist (live-only, NOT now)

1. apex→www 301 + HTTPS (Vercel domain settings)
2. Real 404 HTTP status for unknown paths (verify `curl -I` on live URL)
3. Set `VITE_GA_ID=G-XXXXXXXXXX` in Vercel env before production build
4. Set `VITE_GSC_ID` in Vercel env (or DNS TXT verification alternative)
5. Set `VITE_GBP_URL` once Google Business Profile is verified (feeds JSON-LD sameAs)
6. GSC: submit `https://www.archi-made.com/sitemap.xml`
7. GSC: URL Inspection — request indexing for home + top silo pages
8. Rich Results Test — validate JSON-LD (search.google.com/test/rich-results)
9. Schema Validator — cross-check (validator.schema.org)
10. PSI field CWV p75 mobile — LCP/INP/CLS + long tasks <50ms once traffic accumulates
11. areaServed — DONE in code (Indre-et-Loire + Tours + France); revisit only if market scope changes
12. Durable rate-limit — Vercel KV / Upstash if in-memory limit insufficient
13. OG/social preview — Twitter Card Validator + Facebook Sharing Debugger on live URL
14. Re-export 1200px masters at 2400–3200px for full-bleed heroes (see verify-image-quality.mjs WARN list); rerun variant gen after
15. **Off-site ranking:** follow `docs/off-site-seo-checklist.md` (GSC, GBP, citations — owner-only)

## Journal

- 2026-06-24 — Expertise section copy swap (client request) — In `#expertise` (STUDIO conception): removed the small right-side caption paragraph; moved its text into the prominent left-bordered intro block below the heading (replacing the former 150 m² hook). **Removed:** "Jusqu'a 150 m² de surface de plancher…" hook from the Expertise intro (accepted home keyword reduction; same content remains in FAQ + `/permis-de-construire`). **Now single instance:** "Une approche complète pour préparer, dessiner et visualiser vos projets de construction. / Permis, plans techniques, modélisation 3D : chaque service répond à une étape clé du projet." with host styling (`text-base md:text-xl font-light`, thick left border). git diff = ArchiExpertise copy/layout only (1 caption div deleted, 1 paragraph text swapped, comment updated); accordion/services/animations/SEO layer untouched. Spot-check `/#expertise` desktop+mobile PASS (new text once, old hook absent, heading+accordion intact). Gates: build 18 routes (378.67 KB); seo-check **172/172**; layout-sweep --full **0 REAL** (451 TAP_MINOR); dist dashes **0/0**. Proof PNGs in `./layout-proof/expertise-copy-swap/`. NOT pushed/merged/deployed.
- 2026-06-24 — Hero heading + subcopy typography baseline verification (no edit needed) — Branch `main`. Formal extract baseline `65e09f5` vs HEAD: all targeted typography properties on hero `<h1>` ("Concevoir votre / futur projet") and subcopy `<p>` already byte-identical (`text-[12vw] md:text-[9.5vw] font-bold tracking-tighter leading-[1.1] md:leading-[0.8]`; sub `text-[12px] md:text-lg font-medium leading-tight opacity-70`). **0 typography properties restored.** Out-of-scope deltas left untouched: H1 `mb-12`→`mb-8` (eyebrow fold fix), inner mask `pr-[0.08em] md:pr-[0.12em]` (glyph clip), SEO eyebrow (baseline had none; styling kept). Proof: `./layout-proof/hero-typography-diff.md` + before/after PNGs at {1920,1440,1280,768,390}×900 fresh+repeat; computed styles match baseline (Inter, 700/-9.12px tracking @1920, 18px/500 sub). Deep-link `/#accueil` PASS fresh+repeat desktop+mobile (eyebrow+H1+sub+CTA in view). Gates: build 18 routes (379.31 KB); seo-check **172/172**; layout-sweep --full **0 REAL**; keyword matrix unchanged (dessinateur 58, Tours 48, 5 targets present); eyebrow intact. git diff = proof artifacts + PROGRESS only. NOT pushed/merged/deployed.
- 2026-06-24 — Restore original HOME visible copy (client request) + preserve 100% SEO — Branch note: work landed on `main` (it already contains all of `seo/finalize` plus the 5 approved hero/À-propos sizing commits; switching to `seo/finalize` would discard exactly that approved sizing). Baseline for original wording = `65e09f5`. Built a per-element diagnostic (CONFLICT / SAFE-TO-REVERT / unchanged) and restored ONLY visible text in `src/components/ArchiMadeLanding.tsx`. CONFLICT H2/H1 (old wording back, keyword relocated): Hero H1 "Vos plans de construction, du croquis au permis" → "Concevoir votre / futur projet" (font restored 5.5vw→9.5vw per client; fold re-verified, see below); Expertise H2 "Conception de plans / & permis de construire" → "Studio / conception"; Réalisations H2 "Réalisations en Indre-et-Loire : / plans & rendus 3D" → "L'Art de / Bâtir"; Pourquoi H2 "Dessinateur en bâtiment, à Tours et partout en France" → "Conception. / Projections.". SAFE-TO-REVERT: Hero subcopy → old "Permis de construire, déclarations préalables et plans techniques. / Une approche claire et rigoureuse…"; À-propos lower block → old animated stat gimmick "100% CONFORMITÉ_PC_DP / 0.0 RETARD" (replaces founder card; stat-number counter code at component still drives it). Service cards: 6 titles + descriptions (+ non-visible cat/loc/area) restored to baseline ("Plans d'Exécution", "Dossiers Complets", etc.), KEEPING the SEO-only alt + dedicated-page slug/anchor links. NEW SEO blocks KEPT per decision (hero eyebrow=keyword home, ≤150 m² hook, Zones d'intervention links, FAQ Q4-Q9). INVISIBLE SEO untouched: page titles, meta, OG/twitter, ALL JSON-LD (Damien De Sousa remains only in #founder JSON-LD, 1×, not visible), alts, anchors, the 14 dedicated pages, sitemap. Keyword presence matrix (raw home HTML, none dropped to 0): permis de construire 22, déclaration préalable 13, déclarations préalables 4, plans techniques 8, modélisation 3D 12, accompagnement de projet habitat 1 (anchor+JSON-LD), dessinateur 58, Tours 48, Indre-et-Loire 12. Gates: `npm run build` green 18 routes (max chunk 379.31 KB<500); `seo-check` 172/172 PASS; `layout-sweep --full` 0 REAL (452 TAP_MINOR pre-existing); home dist em/en dashes 0; architecte=3 negation-bound only ("sans architecte"), architecture=0, @type Architect=0. Hero fold probe (9.5vw, 7 viewports incl. 1280×800/1280×720): all ok — eyebrow+H1+subcopy+CTA fully visible, CTA→metadata clearance +62…+281px, no doc overflow-x. NOT pushed/merged/deployed.
- 2026-06-19 — Max on-page SEO (copy + structured-data) — Injected "dessinateur"+geo into the heading/alt layer and added content scaffolding. (1) HOME title→"Dessinateur bâtiment & permis à Tours | ArchiMade Studio"; H1→"Vos plans de construction à Tours, du croquis au permis"; crawlable eyebrow above H1. (2) Filled the 3 keyword-empty display H2s (Expertise→"Conception de plans & permis de construire"; Réalisations→"Réalisations — plans & rendus 3D en Indre-et-Loire"; Pourquoi→"Dessinateur en bâtiment, à Tours et partout en France"). (3) Rewrote img alts with commune/keyword (30 alts now contain "dessinateur"); decorative aria-hidden shell logo keeps alt="". (4) ≤150 m² differentiator hook added near services. (5) "Zones d'intervention" block + visible "Tours (37)" in Contact. (6) FAQ 3→9 (long-tail: surface ≤150 m², DP vs PC, prix, à distance, extension/réno/surélévation, délais mairie). (7) Trust scaffold: founder "Damien De Sousa · Dessinateur en bâtiment · Fondateur"; removed the "0.0 RETARD / 100% CONFORMITÉ" stat gimmick (the only animation removed — it was the targeted gimmick); RCP/affiliation/avis as marked TODO comments (no invented data). (8) Structured-data: Instagram sameAs aligned to live UI handle archi.made.studio; added WebSite node (name "ArchiMade Studio", inLanguage fr); priceRange "€€" on ProfessionalService; still ProfessionalService, no aggregateRating. **CONSTRAINT CONFLICT RESOLVED:** STEP 4/6/7 literal copy used the word "architecte", which violates the CLAUDE.md hard rule + acceptance gate (architecte/architecture must stay 0). Rephrased to convey the ≤150 m² lane positively without the banned word. Gates: build green 4 routes (max chunk 310.90 KB<500); seo-check 25/25 PASS (no Architect); dist scan architecte=0 & architecture=0; dessinateur in title+eyebrow+1 H2+30 alts; 9/9 FAQ questions in raw HTML. **FAQ answers render client-side on expand (existing accordion pattern) → answer text NOT in raw HTML; questions are.** Open TODO(CONFIRM): price 700–1 200 € figures, RCP, affiliation (FNDI?), Google avis, real Instagram handle. Next (separate phases): make FAQ answers crawlable (render collapsed in DOM); dedicated service/location pages.
- 2026-06-20 — Hero regression fix — The 2026-06-19 SEO pass had lengthened the HOME H1 to "Vos plans de construction à Tours, du croquis au permis"; at the hero's large display size (text-[9.5vw]) the extra words wrapped a line, overflowed the hero band, and clipped "…au permis" behind the rotating tagline/cookie bar. Fix (hero ONLY): (a) reverted H1 → "Vos plans de construction, du croquis au permis" (design-fit original); (b) tightened the new eyebrow margin mb-5 md:mb-7 → mb-2 md:mb-3 and H1 margin mb-12 → mb-8 to absorb the eyebrow's added height and restore the original vertical rhythm (needed because 1280×800 only had ~9px original CTA clearance and the eyebrow adds ~37px). **Geo SEO preserved** — no string with "Tours" removed anywhere else: title "Dessinateur bâtiment & permis à Tours…", eyebrow "Indre-et-Loire & à distance partout en France", H2 "Dessinateur en bâtiment, à Tours et partout en France", Zones d'intervention block, and JSON-LD all unchanged. git diff = ArchiMadeLanding.tsx hero block only (H1 string + 2 margin classes + 1 comment); NO nav/CTA/logo/section or any GSAP/framer/Lenis line touched. Headless probe (Playwright, 4 viewports, after preloader detaches) — every width PASS: eyebrow + full H1 (h1HasPermis:true) + rotating tagline + CTA all fullyVisible, docOverflowX:false. CTA-to-fold clearance: 1920×1080 +33px, 1440×900 +50px, 1280×800 +2px, 390×844 +203px. Gates: build green 4 routes (max chunk 310.89 KB); seo-check **25/25**; dist scan architecture=0 & @type Architect=0; dessinateur in title+eyebrow+H2+30 alts (41 total on home). Probe script + screenshots deleted. Next: deploy-day live validation (separate phase).
- 2026-06-20 — All-in-one (crawlable FAQ + scoped architecte + zero-defect layout sweep) — **P1 FAQ crawlability:** refactored ArchiFAQ so every answer node is ALWAYS mounted (was `{open && <motion.div>}` → conditional-mount, absent from prerender). Now a persistent `motion.div` with `animate={open?'open':'closed'}` + height/opacity `variants` (same duration 0.5 / ease [0.16,1,0.3,1] → expand looks identical); collapsed = `aria-hidden` + `inert` (not focusable), button gains `aria-expanded`/`aria-controls`. All 10 FAQ answers now verbatim in raw dist/index.html (10/10). **P2 disclaiming architecte capture:** added ONE FAQ item Q"Ai-je besoin d'un architecte pour mon projet ?" / A"Non : …la loi n'impose pas le recours à un architecte. ArchiMade, dessinateur en bâtiment, …". Rewrote scripts/seo-check.mjs guard (now 4 checks, count preserved): architecture total dist = 0 (hard); @type Architect → FAIL; architecte in title/og:title/twitter:title/meta-desc/H1-H2/JSON-LD → FAIL (self-designation); every architecte occurrence must be negation-bound (its sentence OR the next, on tag-stripped text) → else FAIL. architecte now appears exactly 2× on home — the captured FAQ question + its disclaiming answer — and in NONE of title/og/desc/H/JSON-LD. **P3 exhaustive layout sweep:** built temp Playwright probe (scripts/layout-sweep.mjs, deleted) over 4 routes × 18 widths (incl. just-below-breakpoints 1535/1279/1023/767) × all states (preloader-out, every section scroll, 6 service expands, all 9+1 FAQ expands, gallery modal, cookie shown/accepted, longest-tagline frame, mobile nav). Detectors: horizontal overflow, text–text AABB overlap, clip/truncation, viewport-escape, tap-targets. Iterated: raw 442 "overlaps" → added elementFromPoint occlusion test (kills back-face `backface-visibility:hidden` "Processing Data", overlay-occluded, stacked anim dupes) → 6 → whitelisted the ARCHIMADE decorative watermark (gave it `aria-hidden`, real a11y win) + the intentional `archi-sticky-contact` floating CTA (mix-blend-difference, overlay-UI category) → 3 (each 6px² adjacent-nav line-box grazes) → added line-leading tolerance (overlap must exceed 3px on BOTH axes, not a degenerate sliver) → **0 overflow / 0 overlap / 0 clip / 0 escape in EVERY cell**. Only residual = MINOR sub-40px tap heights on 5 intended controls (hamburger, cookie Refuser/Accepter/Détails, mobile-menu email) — design-intended, out of scope. Gates: build green 4 routes (max client chunk 311.26 KB<500); seo-check **28/28**; architecture=0; @type Architect=0; dessinateur in title+eyebrow+H2+30 alts; H1+permis+eyebrow+Zones+founder intact; JSON-LD WebSite+ProfessionalService+priceRange €€+sameAs archi.made.studio, no Architect, no aggregateRating; service/about-step descriptions confirmed crawlable. git diff = FAQ refactor + new item + seo-check guard + 1 aria-hidden (no GSAP/ScrollTrigger/framer/Lenis logic line touched). Probe + screenshots deleted. **TODO(client):** RCP, affiliation (FNDI?), Google avis, price 700–1 200 € figures, experience/project-count metrics — still flagged placeholders, none invented.
- 2026-06-20 — 100% finish (dash-free copy + real nav/logo overlap fixes + true-zero-visible layout + kept proof-sheet) — **P1 dashes:** removed all 40 em/en-dash separators from src (40 -> 0; dist 0). Copy restructures: Réalisations H2 "Réalisations en Indre-et-Loire : plans & rendus 3D"; Zones "Basé à Tours (37), interventions…"; 6 gallery/about alts "…, dessinateur ArchiMade"; hero desc, founder line, legal éditeur/activité/footer, all alts -> "," / ":" by readability. Place-name hyphens (Saint-Cyr-sur-Loire, Indre-et-Loire) + middots (· eyebrow/founder) untouched. **P2 detector rewrite + real fixes:** scripts/layout-sweep.mjs now models VISIBLE INK overlap (Range glyph-rects with line-leading inset, effective opacity >=0.5 incl. ancestors, color-alpha >=0.5; Z-ORDER NEVER excuses; elementFromPoint used ONLY to confirm a 3D backface rotated away). Caught what the old occlusion-based pass wrongly dismissed. **Three real fixes (CSS only, no animation logic touched):** (A) nav vs CONTACT heading + (B) logo vs ZONES: Contact `<footer>` inner container `xl:max-w-none xl:mx-0 xl:pl-72 xl:pr-16`, columns `lg:flex-row xl:flex-col 2xl:flex-row` (clears the fixed 25vw sidebar at xl, form stacks through xl, side-by-side restored at 2xl; left col `xl:w-full 2xl:flex-1`, form `xl:w-full 2xl:w-145 2xl:shrink-0`). (C) nav vs long right-aligned "Réalisations en Indre-et-Loire :" heading (a side-effect of the P1 restructure): gallery-header `xl:pl-32` so it wraps clear of the nav. Plus the sticky "Nous contacter" pill became an icon-only circle until xl (`hidden xl:block` text / `xl:hidden` icon / circle sizing to xl) so it stops covering FAQ/content text at md/lg; at xl it's the rotated text pill in the right gutter. **Sweep result: 0 overflow / 0 overlap / 0 clip / 0 escape** across 4 routes × 18 widths (incl. just-below-breakpoints) × all states (sections, 6 service + 10 FAQ expands, gallery modal, cookie shown/dismissed, longest tagline, mobile nav); probe destroys Lenis so scroll-direction UI (mobile header auto-hide, isUIHidden sidebar fade over Réalisations/Expertise) fires as a user sees it. Residual = MINOR sub-40px tap heights on intended controls only. **P3 proof-sheet (KEPT):** ./layout-proof/ 38 PNGs — home {default, contact, faq-open, modal-open, cookie-shown, mobile-nav≤768} at 1920/1440/1280/1024/768/390 + 3 legal routes at 1280/390. Gates: build green 4 routes (max client chunk 311 KB<500); seo-check **28/28**; dist architecture=0, @type Architect=0; architecte only in disclaiming FAQ (negation-bound, 2x = captured question + answer, none in title/og/H/JSON-LD); dessinateur in title+eyebrow+H2+30 alts; all FAQ answers crawlable; eyebrow + full H1 (du croquis au permis) intact; em/en dashes 0 in dist+src, place-name hyphens intact. git diff = copy punctuation + layout CSS + sweep script; no GSAP/ScrollTrigger/framer-motion/Lenis logic line altered. **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics.
- 2026-06-20 — Contact: clearance-only overlap fix, restore balanced side-by-side composition — commit 65376dc had fixed the Contact nav/logo overlaps but WRONGLY restructured the columns (xl:flex-col stacking + 2xl:flex-row, lg:w-1/2->xl:w-full->2xl:flex-1 width surgery, xl:w-full/2xl:w-145 form surgery, xl:max-w-none/xl:mx-0/xl:pl-72 decenter), breaking the balanced side-by-side composition. **Reverted** all of that: Contact `<footer>` columns back to the original `flex flex-col lg:flex-row justify-between items-start lg:items-center gap-12 lg:gap-20`, left col `lg:w-1/2 space-y-10`, form `contact-form-card lg:w-125 xl:w-145 w-full` — byte-identical to pre-65376dc. **Re-fixed by CLEARANCE ONLY** (padding, no restructure): container `+ xl:pl-[max(96px,calc(930px-50vw))]` — extra left padding that only kicks in below ~1668px (centered max-w-7xl already clears the fixed nav/logo on wide screens, so wide composition is byte-identical/0 shift) and clears the nav (~215px) + bottom logo (~260px) at narrow xl. STEP 3 scale: CONTACT heading `+ xl:text-[clamp(3.75rem,calc(14vw-7.5rem),6rem)]` so it fits the nav-cleared left column at narrow xl and returns to full 8xl by ~1536. NO flex-direction/column-count/stacking/relative-width change (composition guard confirmed vs 65376dc^). Réalisations gallery-header `xl:pl-32` (from prior task) retained. Sweep (visible-ink, z-order-never-excuses, Lenis destroyed): **0 overlap / 0 overflow / 0 clip / 0 escape** across 4 routes × 18 widths × all states. Proof-sheet regenerated (./layout-proof/ 38 PNGs) — Contact at 1280/1440/1920 show the BALANCED side-by-side (info left, form right), zero overlap. Gates: build green 4 routes (max chunk 311 KB<500); seo-check **28/28**; dist+src em/en dashes 0, place-name hyphens intact; architecture=0, @type Architect=0, architecte only in disclaiming FAQ (negation-bound); dessinateur in title+eyebrow+H2+30 alts; FAQ answers crawlable; eyebrow + full H1 intact. git diff = Contact container padding + responsive heading scale only; no animation logic line altered. **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics.
- 2026-06-20 — Contact: align form card to heading top line (balanced side-by-side) — alignment-only polish. Measured form-card.top vs CONTACT-h2.top in the Contact `<footer>`: BEFORE (lg:items-center centered the row, form card is the taller column) the form poked ABOVE the heading by ~38px (1280 -45px, 1440 -34px, 1920 -38px). FIX: removed `lg:items-center` so the row stays `items-start` (top-aligned) at all breakpoints → form-card.top == heading.top. AFTER: offset **0px** at 1280/1440/1920 (form top and CONTACT heading top share one clean line; form no longer raised). NO width/flex-direction/stacking/font/copy change (single class removed: `lg:items-center`). Sweep: **0 overlap / 0 overflow / 0 clip / 0 escape** across 4 routes × 18 widths × all states. Proof-sheet refreshed (./layout-proof/ 38 PNGs); Contact 1280/1440/1920 show the form top aligned to the heading top, balanced. Gates: build green 4 routes (max chunk 311 KB<500); seo-check **28/28**; dist+src dashes 0; architecture=0, @type Architect=0; eyebrow + full H1 intact; FAQ answers crawlable; dessinateur in title/eyebrow/H2/30 alts. git diff = single Contact vertical-alignment class; no animation logic touched. **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics.
- 2026-06-20 — Contact: drop form card below heading, level with intro text — top-offset-only polish. The prior change top-aligned the form to the CONTACT heading; this drops it to the intro-paragraph line so the heading stands alone above and the form sits lower beside the supporting text/contact info. Measured (viewport h=1024, scrolled to #contact): needed drop = intro.top - form.top = heading-height + the space-y-6 gap (1.5rem) = 120px@1024, 84px@1280, 106px@1440, 120px@1920 (tracks the heading clamp). FIX (form card only, row stays items-start): added `lg:mt-[7.5rem]` (lg heading = text-8xl) + `xl:mt-[calc(clamp(3.75rem,calc(14vw-7.5rem),6rem)+1.5rem)]` (matches the xl heading clamp). AFTER: form.top == intro.top within **0px** at 1024/1280/1440/1920; form clearly below the heading; form.bottom (782-800px) well within the lg:h-screen overflow-hidden section (1024) — NO clip/overflow. NO width/flex-direction/stacking/font/copy change (only two `mt` utilities appended to the form card). Sweep: **0 overlap / 0 overflow / 0 clip / 0 escape** across 4 routes × 18 widths × all states. Proof-sheet refreshed (./layout-proof/ 38 PNGs); Contact 1280/1440/1920 show the form lower, top level with the intro text. Gates: build green 4 routes (max chunk 311 KB<500); seo-check **28/28**; dist+src dashes 0; architecture=0, @type Architect=0; eyebrow + full H1 intact; FAQ answers crawlable; dessinateur in title/eyebrow/H2/30 alts. git diff = form-card top-offset classes only; no animation logic touched. **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics.
- 2026-06-20 — Contact: recompose to self-balancing layout (heading on top, form below beside info) — ended the per-element margin nudging by fixing the STRUCTURE. **Before:** the CONTACT `<h2>` lived inside the left column (sibling of the form), so aligning the form to the heading looked too high and dropping it via margin risked hiding the heading on scroll. **After:** moved `<h2 contact-title>` OUT to a FULL-WIDTH block at the TOP of the container (above the row, `mb-12 lg:mb-16`, keeps the xl:pl nav clearance + clamp); below it ONE row `flex flex-col lg:flex-row items-start` with left col = contact info (intro `contact-desc` + email/phone + ZONES) and right col = form card. Removed the `space-y-6` heading wrapper (intro is now a direct left-col child) and DELETED the manual form offsets `lg:mt-[7.5rem]` / `xl:mt-[calc(clamp(...))]` — the form now aligns to the intro paragraph automatically by being a sibling of the row, not the heading. Section sizing: `lg:py-0 lg:h-screen` → `lg:py-28` (kept `min-h-screen` + `flex justify-center` + the `lg:sticky lg:bottom-0` reveal) so the whole composition centers with breathing room and nothing is cropped. **Measured (h=1024, scrolled to #contact):** heading.top≈112 (fully visible, ~112px above), form.top==intro.top within **0px** at 1024/1280/1440/1920, heading always above the form, form.bottom 842-879 < 1024 (no crop). Side-by-side preserved at lg+; stack-below-lg preserved (mobile 390/768 stack heading→info→form, form clears the cookie bar). NO column-width surgery (`lg:w-1/2`, `lg:w-125 xl:w-145` unchanged); NO GSAP/ScrollTrigger/framer-motion/Lenis logic touched — contact reveal still fires (`.contact-title` opacity 1 after scroll; class-based GSAP targets intact). Clearances intact (nav left, logo bottom-left, "Nous contacter" tab right). Sweep: **0 overlap / 0 overflow / 0 clip / 0 escape** across 4 routes × 18 widths × all states. Proof-sheet refreshed (./layout-proof/ 38 PNGs) incl. full-section Contact at 1920/1440/1280/1024/768/390 in BOTH cookie-shown and cookie-dismissed states (heading on top + info + form, balanced, nothing cut). Gates: build green 4 routes (max chunk 311 KB<500); seo-check **28/28**; dist+src dashes 0; architecture=0, @type Architect=0; CONTACT heading crawlable; eyebrow + full H1 intact; FAQ answers crawlable; dessinateur title/eyebrow/H2/30 alts. **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics.
- 2026-06-20 — Contact + footer: full proportional re-architecture (grid, 5:7 band, integrated footer) — rebuilt the closing composition to one cohesive grid. **CONTENT GRID (shared by content + footer):** `max-w-7xl mx-auto px-6 md:px-16 lg:px-24` + `xl:pl-[max(96px,calc(930px-50vw))]` (nav clearance); footer lives in the same container so its edges align to the content insets (copyright on the left inset, links on the right inset). **VERTICAL (lg+, centered group in min-h-screen, py-16 symmetric):** (1) CONTACT heading full-width on top (clamp, cleared from nav); (2) gap heading→band `mb-8 lg:mb-10`; (3) two-column band TOP-ALIGNED `items-start`, **5:7 ratio** via `lg:flex-[5]` (info ~42%) | `lg:flex-[7]` (form ~58%) — flex-basis 0 + grow makes it gap-aware and exact; column gap `gap-12 lg:gap-20` (3rem/5rem); info column `space-y-8` (intro → 2rem → email+phone → 2rem → ZONES); email/phone switched to `flex-wrap` (no `whitespace-nowrap` min-width forcing) so the 5:7 holds at every width; (4) gap band→footer `mt-8 lg:mt-10`; (5) FOOTER band: hairline `border-t`, `© 2026 ArchiMade Studio · France` (left, muted text-black/40) + `Mentions légales · Confidentialité · Cookies` (right, middot-separated, hover), `pt-8`; (6) bottom py == top. **PROPORTIONS chosen & why:** ratio **5:7** kept exact (reads as deliberate: info as supporting column, form as the action). The three gaps were compressed below the blueprint's nominal 3rem/5rem-7rem because the form card is **606px** tall — to fit the WHOLE composition (heading+band+footer) in one 1024-tall viewport AND keep the footer above the fixed cookie overlay (its top ≈934px at 1024), heading→band and band→footer are 32/40px (the nominal 80/112 would push the footer into the cookie bar). Column gap (3rem/5rem) + 5:7 kept as specified. **Measured (h=1024):** head.top 98-116 (visible, breathing room); info-top==form-top **0px** at 1024/1280/1440/1920; ratio **42:58** at all; footer.bottom 908-927 ≤ cookieTop 934 (clears overlay); fits one viewport, nothing cropped. Below lg: auto-height stack heading→info→form→footer, form+footer clear the cookie bar, links wrap. **Preserved:** heading/form-card/nav/logo/tab/input styles unchanged; clearances intact (nav/tab/logo); all GSAP reveals fire (`.contact-title/.contact-desc/.contact-info-item/.contact-form-card/.contact-footer-bar` opacities all 1 after scroll); NO GSAP/ScrollTrigger/framer-motion/Lenis logic touched. Sweep: **0 overlap / 0 overflow / 0 clip / 0 escape** across 4 routes × 18 widths × all states. Proof-sheet refreshed (./layout-proof/ 38 PNGs) incl. full Contact+footer at 1920/1440/1280/1024/768/390 in cookie-shown + cookie-dismissed. Gates: build green 4 routes (max chunk 311 KB<500); seo-check **28/28**; dist+src dashes 0 (middot · is a design separator, not a dash); architecture=0, @type Architect=0; CONTACT crawlable; eyebrow + full H1 intact; FAQ crawlable; dessinateur title/eyebrow/H2/30 alts. **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics.
- 2026-06-20 — Contact: fill left info column to match form height (enlarged + distributed, height-aware, footer unchanged) — killed the dead space under the LEFT info column (it ended at ZONES while the taller form card extended ~110px lower). **Structural fix:** band row `items-start` → `items-start lg:items-stretch` so at lg+ the info column takes the FULL band height; info column `space-y-…` → `flex flex-col justify-between gap-[clamp(0.5rem,2vh,1.5rem)]` so its three blocks (intro / email+phone / ZONES) distribute top→bottom over that height. **Enlarged (height-aware, FLOORS = prior values):** intro `clamp(0.8rem,1.95vh,1.375rem)`, email/phone `clamp(0.9rem,1.95vh,1.3rem)`, ZONES `clamp(0.72rem,1.6vh,1rem)`; pt separators already vh-clamped. Floors kept = pre-edit so the tight h=700 cells (footer only ~4-5px above the cookie bar) are byte-unchanged; only the vh-slope/caps lift, so copy grows on tall screens where the form leaves headroom. **Why this is safe (the geometry):** with `items-stretch` the band height = max(info, form); keeping info INTRINSIC ≤ form means the FORM keeps driving the band, so the band (and footer) never grow — `justify-between` only spreads existing blocks into the gap. **DISCOVERY:** the working tree carried a PRIOR session's _uncommitted_ height-aware clamp rework of the whole Contact section (HEAD/59f632f still had `text-8xl` + fixed `py`/`p-8`/`py-4` form, `lg:sticky lg:bottom-0`, `min-h-screen`); this commit lands that rework together with the column fill (one dirty file). **Measured (Playwright, strict port, after preloader+scroll; before vs after):** at the proof width **1440 the footer is byte-identical** (700→605, 768→658, 800→683, 900→763, 1024→851 — all == before, all ≤ cookie top); info-col height == form-card height within **1px (colGap −1)** at every lg+ cell (dead space gone); fit/footClr/subClr **PASS at all 20 cells** (1024/1280/1440 × 700/768/800/900/1024 + 390 mobile). Residual: at 1280/1024-width × tall heights the info naturally fills/exceeds the form (it did so even before — narrow column wraps the city list), so the footer sits +11/+32px lower there, still well above the cookie bar (margin ≥51px) → fit holds. **Sweep (visible-ink, z-order-never-excuses, Lenis destroyed):** `scripts/layout-sweep.mjs` **0 overflow / 0 overlap / 0 clip / 0 escape** across 4 routes × 18 widths × all states (only intended sub-40px TAP_MINOR controls). Proof shots added: ./layout-proof/home-1440x{768,900,1024}-contact-cookie-shown.png — left column visibly fills its height, balanced, footer + cookie clear, nothing clipped. Gates: build green 4 routes (max client chunk 311.71 KB<500); seo-check **28/28**; dist em/en dashes **0**; architecture=0; @type Architect absent; eyebrow + full H1 (du croquis au permis) intact; FAQ answers crawlable. No GSAP/ScrollTrigger/framer-motion/Lenis logic touched (all `.contact-*` reveal hook classes preserved). **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics.
- 2026-06-21 — Réalisations: hide left nav + logo while gallery in view (immersive, restored on leave) — the fixed left nav (links + Instagram float) AND the bottom-left logo overlapped the full-bleed masonry of #realisations and became unreadable. **Trigger (GSAP ScrollTrigger, reuses the site's existing mechanism):** added a trigger on `#realisations-content` (the image GRID, not the section — its tall right-aligned heading already clears the nav) with `start:"top bottom"` → `end:"bottom top"`, i.e. hide from the instant the first image enters at the bottom (where the logo sits) until the last image leaves past the top. These opposite-extreme bounds give wide hysteresis → no flicker at either boundary; onEnter/onLeave/onEnterBack/onLeaveBack handle BOTH scroll directions. **Composition (no double-toggle):** the old single `isUIHidden` boolean (one setter shared by the Expertise + Réalisations triggers) was the latent bug — one section's onLeave could reveal the UI while the other still wanted it hidden. Refactored to per-section flags `isExpertiseInView` / `isGalleryInView`, with `isUIHidden = isExpertiseInView || isGalleryInView`; each trigger owns ONLY its own flag, so they can never fight over one boolean → no double-toggle, no stuck-hidden. Expertise trigger behavior byte-identical (same thresholds, just renamed setter). **What's hidden + a11y:** the sidebar container (`ArchiNav` + bottom-left `ArchiLogo`, already opacity/translate-faded via the existing `transition-all duration-500` UI-fade on `isUIHidden`), both `ArchiInstagramFloat` anchors, and the right `archi-sticky-contact` tab — all gained `aria-hidden={isUIHidden}` + `inert={isUIHidden}` so while faded they are non-focusable / non-interactive, restored in BOTH directions. **Mobile:** the `xl:hidden` `ArchiHeader` auto-hides on scroll-down but RE-APPEARS on scroll-up — which would slide its logo+Menu over the gallery when scrolling back up through Réalisations; added a `galleryInView` prop (`hidden = isHidden || galleryInView`) + `aria-hidden`/`inert` so it stays hidden over the gallery and resumes normal auto-hide elsewhere. **Gallery untouched** (stays full-bleed — the fix hides the overlapping UI, never insets/shrinks the grid). **Verify:** proof shots regenerated — `home-{1440,1280,390}-realisations-nav-hidden.png` show NO nav/logo/tab over the images; `home-1440-nav-restored.png` (#propos) shows the full left nav (À propos/Méthode/Réalisations/Expertise/FAQ/Contact + Instagram) + bottom-left ARCHIMADE logo back and interactive. Sweep (`scripts/layout-sweep.mjs`, visible-ink, Lenis destroyed so scroll-direction UI fires as a user sees it): **0 overflow / 0 overlap / 0 clip / 0 escape** across 4 routes × 18 widths × all states — specifically 0 nav-or-logo-over-gallery overlap; only residual = intended sub-40px TAP_MINOR controls (Menu, cookie Détails/Refuser/Accepter, contact email). Gates: build green 4 routes (max client chunk 311.92 KB<500); seo-check **28/28**; architecture=0; @type Architect absent (the only `architecte` hits are the disclaiming FAQ Q+A, negation-bound); em/en dashes 0; FAQ answers crawlable; eyebrow + full H1 (du croquis au permis) intact; Contact section unchanged. git diff = ArchiMadeLanding.tsx only — the per-section flag refactor + Réalisations ScrollTrigger + mobile-header force-hide + aria-hidden/inert; NO GSAP/ScrollTrigger/framer-motion/Lenis animation logic line removed or reordered (Expertise trigger preserved). **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics.
- 2026-06-21 — Max controllable SEO: structured-data (founder+name+logo), sitemap lastmod, strengthen plans techniques, heading hygiene, preloader LCP — pushed every on-site lever to its ceiling. **PART A (JSON-LD @graph):** Organization name "ArchiMade" → "ArchiMade Studio" (+ ProfessionalService name aligned too) killing the entity-name drift (legalName stays ARCHI-MADE LTD on both); added a Person node `{Damien De Sousa, jobTitle "Dessinateur en bâtiment", @id #founder}` linked as Organization.founder + ProfessionalService.founder/employee (public RNE/INPI dirigeant, no invention); Organization.logo upgraded string → ImageObject (url + width/height 1254×1254 from the variant manifest). geo SKIPPED with `TODO(geo)` comment (lat/long not derivable offline without invention). Kept WebSite + ProfessionalService + 7 Service nodes, priceRange "€€", sameAs archi.made.studio; NO Architect, NO aggregateRating. JSON-LD parses (validated). **PART B (sitemap):** added `<lastmod>` to all 4 URLs in public/sitemap.xml; scripts/prerender.mjs now refreshes lastmod to the real build date at build (stamped 2026-06-21) so it never goes stale. **PART C (weakest term + thin long-tail, text-only, woven into EXISTING blocks):** "plans techniques" body 2 → **4** (added to the ≤150 m² hook + the "Quels documents" FAQ answer); "maison individuelle" 1 → **3** (≤150 hook + disclaiming FAQ); added "sans architecte" ×1 (negation-bound in the architecte FAQ) and "tarif" ×1 (prix FAQ, figures still TODO-client). architecte body 2 → 3, ALL negation-bound (seo-check green). No new sections, no layout change, no stuffing, H1/eyebrow untouched. **PART D (heading hygiene):** decorative `text-[25vw]` ARCHIMADE watermark `<h2>` → `<span class="block …>` (0 visual change, flex item) so it stops polluting the heading outline; H2 count 13 → 12, watermark no longer a heading. **PART E (preloader LCP fix — the carve-out):** the opaque intro overlay blocked the hero ~3.8s + a trailing 1.5s container fade (~5s to full hero) → mobile LCP failed. Compressed the GSAP timeline (logo 1.5→0.6s, char start 0.8→0.25 / stagger 0.06→0.018 / reveal 0.8→0.45 / lines 0.3→0.14 / char-inner 0.6→0.35, progress 3.5→1.3s), removed the redundant sequential content-exit tween, and shortened the single container dissolve 1.5→0.4s; the intro still plays the same blueprint choreography, just faster. Added a sessionStorage gate (`archimade_intro_seen`) so repeat visits in a session skip the intro for an instant hero — initial state kept deterministic (matches prerendered HTML) and only dropped AFTER hydration, so NO hydration mismatch (verified: 0 console errors, 0 hydration warnings). **Measured cold-load (Playwright, fresh context):** unthrottled hero visible **1859 ms mobile / 1941 ms desktop (≤2.0s ✓)**; **LCP 264 ms mobile / 116 ms desktop** (the preloader logo is the LCP element, painting sub-second — far under the 2.5s budget). Even at 4× CPU throttle LCP stayed 612 ms. Intro frame at 700ms confirmed the logo + drafting grid + char animation still render. **Gates:** build green 4 routes (max client chunk 312.55 KB<500); seo-check **28/28**; dist architecture=0; @type Architect absent; architecte only negation-bound FAQ; dist em/en dashes **0** (4 remain in src CODE COMMENTS from the prior commit — not shipped, out of this diff's scope); dessinateur in title/eyebrow/H2/30 alts; eyebrow + full H1 intact; "plans techniques" body **4**. layout-sweep **0 overflow / 0 overlap / 0 clip / 0 escape** across 4 routes × 18 widths × all states (Contact unchanged). git diff = JSON-LD + sitemap + prerender stamp + woven copy + watermark tag + preloader timing only; no other animation logic touched. **At controllable ceiling now:** structured data, on-page keywords/intent, rendering, heading hygiene, sitemap, cold-load LCP. **Remaining (NOT on-site-controllable here):** dedicated service/commune pages (Tier-3 content) + launch-gated GBP/GSC/reviews/citations + off-site authority. **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics; TODO(geo) for GeoCoordinates.
- 2026-06-21 — Dedicated pages: service + location silo (the largest remaining organic lever) + dash cleanup — built a 14-page silo on ONE reusable template, lifting the prior no-new-routes rule. **IA (14 routes):** 6 service pages (`/permis-de-construire`, `/declaration-prealable`, `/plans-techniques`, `/modelisation-3d`, `/rendus-photorealistes`, `/accompagnement-projet-habitat`) + 8 location pages (dept hub `/dessinateur-batiment-indre-et-loire`, city hub `/dessinateur-batiment-tours`, + 6 communes with REAL réalisations: joue-les-tours, saint-cyr-sur-loire [2 projets], chambray-les-tours, montlouis-sur-loire, veigne, esvres). Communes WITHOUT a genuine project (no doorway pages) were NOT created. **Architecture:** `src/data/pages.ts` (typed content, single source of truth) + `src/components/DedicatedPage.tsx` (reuses LegalPage visual language: dark theme, grain, fixed nav, crawlable FAQ, breadcrumb, related cards, devis CTA, footer). Routes wired in `src/App.tsx` (`DEDICATED_PAGES.map` + `PRERENDER_ROUTES` spread); `scripts/prerender.mjs` now GENERATES `dist/sitemap.xml` from the route list (priority by type, auto lastmod) so it can never drift. **Per page (all unique):** own title/meta/canonical/og/twitter, exactly one H1 with primary keyword + geo, 400-600 words of genuinely unique copy (service process/scope or the commune's real project), unique 2-4 item crawlable FAQ (always-mounted, CSS-collapsed), BreadcrumbList + Service JSON-LD whose provider links the existing Organization @id (exported ORG_ID/BUSINESS_ID from StructuredData.tsx). **Internal linking (bidirectional, keyword anchors):** home service accordion items now link to their service page (always-rendered href, visible when expanded, no orphan); Zones d'intervention block links to both hubs + all 6 communes; every sub-page links home + 2-3 related + the contact CTA (`/#contact`, with a new incoming-hash smooth-scroll on home). **Cleanup:** removed the 4 em-dashes from src comments (src + dist now 0). **Gates (verbatim):** `npm run build` green, **18 routes** prerendered (max client chunk 365 KB < 500); `node scripts/seo-check.mjs` **164 PASS / 0 FAIL** (per-page unique title/meta/H1/canonical, BreadcrumbList present, Service schema parses, word count 400-700 each, no Architect, architecture=0, architecte only negation-bound, dashes=0 in dist, home links to all 14 silo pages, each page to home+contact+related); layout-sweep over **18 routes × 18 widths × all states** = **0 overflow / 0 overlap / 0 clip / 0 escape** (only intended sub-40px TAP_MINOR). New pages keep eager+fetchpriority hero (no preloader on sub-pages, content above fold). git diff scoped to pages.ts + DedicatedPage.tsx + App.tsx + StructuredData.tsx (@id exports) + ArchiMadeLanding.tsx (service-link + zones-links + hash-scroll + dash cleanup) + prerender.mjs + seo-check.mjs + public/sitemap.xml; home design + animations untouched. **Pre-existing uncommitted files from a prior session (.env.example, index.html, package\*.json docx dep, vite.config.ts reformat, measure.mjs, docs/reports/) left out of this commit (out of scope).** **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics; TODO(geo) for GeoCoordinates.
- 2026-06-21 — Layout-sweep performance refactor — default gate now ~33s (was ~16 min full / 3h+ when networkidle stalled). **Fixes:** replaced `networkidle` → `domcontentloaded` + `#root` + hero opacity wait; one browser context per route with `setViewportSize()` (not 324 cold contexts); home section settle uses opacity `waitForFunction` (400–800ms cap, not blind 1800ms); per-cell progress logs (`[sweep n/N]`); `__MOBILE` set after each navigation (bugfix — was reset by init script). **Default mode:** 6 representative widths (1920/1280/1024/768/390/360) × 4 routes (home + 2 silo templates + 1 legal); reduced home states (contact section, 1 service expand, 1 FAQ, gallery/cookie/mobile-nav). **`--full` flag:** prior exhaustive 18×18 × all routes × full home state machine. **Gates:** build green 18 routes (max chunk 376.77 KB); seo-check **164/164 PASS**; default sweep **0 REAL / 24 TAP_MINOR** in **32.7s**; preview server stopped after run. **M1:** measured all 18 meta descriptions in source + dist; 8 flagged routes already ≤155 decoded chars (max 154), no copy trims required. **M2:** home @graph 7 Service nodes each carry areaServed (Indre-et-Loire + Tours + France), one-sentence description, provider @id → #organization; 6 nodes link url to dedicated pages; conception-de-plans stays url-less. **M4:** prerender.mjs demotes boot-shell logo to fetchpriority=auto when SSR hero already has high; exactly one fetchpriority=high per route (home/legal = logo, silo = hero). **M7:** service accordion anchors keyword-aligned (dropped "En savoir plus :" prefix); plans link anchor → "Plans techniques à Tours" (à valider par Fayçal). **M8:** Zones d'intervention links rewritten to descriptive form ("Dessinateur à/en …", 0 bare-city anchors). **N1:** all 18 titles end "| ArchiMade Studio". **N3:** robots meta index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1 on all 18 via Seo.tsx. **N4:** ProfessionalService email contact@archi-made.com. **N5:** theme-color #0a0a0a all routes. **Held (deliberate):** M3 GeoCoordinates, M6 home H1/hero byte-unchanged. **Gates:** build green 18 routes (max client chunk 376.77 KB<500); seo-check **164/164 PASS**; layout-sweep **18 routes × 18 widths = 0 REAL** (1133 TAP_MINOR intentional); src+dist em/en dashes **0**; home H1 "Vos plans de construction, du croquis au permis" invariant; architecte only negation-bound FAQ; architecture=0; @type Architect absent. layout-sweep.mjs now auto-discovers all prerendered routes from dist. **TODO(client) unchanged:** RCP, affiliation, Google avis, price figures, experience metrics; TODO(geo) for GeoCoordinates.
- 2026-06-22 — Finalize A/B/C (opening hours + service-area model + ad-addressable section anchors) — **PART A:** added `openingHoursSpecification` (Mo-Fr 09:00-18:00, no weekend) to the ProfessionalService node in StructuredData.tsx; JSON-LD parses. **PART B (no public premises -> service-area business):** ADDRESS MAP — the établissement street ("25 rue du Maréchal Ney") appeared in 4 spots: (1) StructuredData ProfessionalService PostalAddress, (2) LegalPage mentions, (3) pages.ts Tours page body para, (4) pages.ts Tours page FAQ answer. After: REDUCED the marketing PostalAddress to addressLocality "Tours" + addressRegion "Indre-et-Loire" + addressCountry "FR" (dropped streetAddress + postalCode — geographic signal only, valid PostalAddress); reworded the two Tours-page visible strings to drop the street + the "rendez-vous sur place" premises framing (now "implanté à Tours, en Indre-et-Loire", échanges à distance); KEPT the full street address ONLY in /mentions-legales (legal requirement). GeoCoordinates stays permanently out (replaced the old TODO(geo) with a permanent-omit comment). seo-check NAP/address gate ADDED (7 checks): street absent from all non-legal routes + present on mentions-legales; 0 GeoCoordinates; email byte-identical everywhere (contact@archi-made.com); phone byte-identical everywhere (+33624896695); home shows canonical email+phone; openingHours present. dist verified: home address = {locality Tours, region Indre-et-Loire, country FR}, 0 streetAddress/postalCode/GeoCoordinates in ANY of 18 dist HTML. **PART C (ad-addressable sections):** renamed section ids to the PERMANENT canonical set #accueil / #a-propos / #methode / #realisations / #expertise / #faq / #contact (was hero/propos/methodes); updated nav slugs (desktop + mobile overlay), hero-click + getElementById refs, and layout-sweep id list. New module-level `scrollToSection(slug)` smooth-scrolls via Lenis (element passed -> scroll-margin-top supplies the header offset, no double offset) AND `history.pushState`'s the hash (shareable URL, no native jump); desktop nav + mobile overlay both wired through it (mobile defers 80ms so body overflow:hidden is released first). Header offset = `scroll-margin-top:96px` under xl / 0 from xl up (left sidebar, no top bar) in index.css — single source, honoured by both Lenis-element and scrollIntoView; deliberately NO css scroll-behavior:smooth (Lenis owns motion). Deep-link on load now passes the ELEMENT to lenis.scrollTo inside the post-`isLoading` effect (fires AFTER intro completes, or instantly when archimade_intro_seen) with double-rAF settle. **Headless probe (Playwright, deleted): 11/11 PASS** — deeplink /#contact + /#realisations on fresh-intro AND repeat, desktop + mobile (sections land at top 0 desktop / ~96 mobile, header auto-hidden, hash present); navclick #a-propos/#methode/#faq desktop+mobile (hash updates, 96px mobile offset, 0 desktop). Bug found+fixed mid-pass: Lenis double-applied the offset (manual -96 + scroll-margin 96 = 192) when passed an element -> removed the manual JS offset, rely on scroll-margin only. Gallery-nav-hide + Expertise UI-hide triggers (#realisations-content / #expertise) untouched (scroll-margin doesn't affect getBoundingClientRect); mobile menu whitelist intact. **Gates:** build green **18 routes** (max client chunk 377.36 KB<500); `seo-check` **171 PASS / 0 FAIL** (+7 NAP/address); `layout-sweep --full` **18×18 = 0 REAL** (467 intended TAP_MINOR); src+dist em/en dashes **0/0**; 0 @type Architect, 0 "architecture", architecte negation-bound only; all 18 titles end "ArchiMade Studio"; sameAs identical (1 value); sitemap 18/18; home H1 + hero composition + Contact/footer byte-unchanged. **Site back at the full controllable ceiling with the new info wired in.** **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics.
- 2026-06-22 — À propos: alignment + height-aware clearance fix (layout only, no restructure) — surgical fix to the home #a-propos lower row only. **Diagnosis (BEFORE proof-sheets /#a-propos × {1920,1440,1280,768,390} × {1024,900,800,768,700}, fresh+repeat):** the right Damien De Sousa column was `justify-end` inside the xl stretched row, so on desktop it floated to the BOTTOM of the row, its border-top/name landing ~200px BELOW the intro paragraph top (ragged, disconnected) instead of aligned with it; the `mt-16 md:mt-24` heading->row gap (64/96px) was oversized for short heights. NO real section clipping ever existed (section bottom always ~1000px below Damien; the section grows height-aware into #methode) and heading+intro start were already in view; the dark/Instagram circles over Damien at the landing snapshot are FIXED overlays (sticky-contact icon + Instagram float, whitelisted) not section layout. **Fix (2 classes, ArchiMadeLanding ArchiAbout only):** (A) right column `justify-end` -> `justify-start` so it top-aligns with the intro paragraph (border-top line now level with "ArchiMade accompagne", columns share aligned tops, no floating block); (B) row `mt-16 md:mt-24` -> `mt-[clamp(2.5rem,6vh,6rem)]` (floor 2.5rem prevents cram, cap 6rem == old md:mt-24 so tall desktop rhythm unchanged) lifting the intro up on short viewports (1280: intro.top 480->432; 1920: 401->~360). Height-aware: section never forced to one viewport; Damien below the fold at small heights is reached by a short scroll, never clipped. Heading font size untouched (design preserved). **AFTER proof-sheets** kept in ./layout-proof/about-before + ./layout-proof/about-after (25 cells × fresh/repeat): desktop shows Damien top-aligned to intro; stacked mobile/tablet reads as one composition (consistent 24/40px left edges); heading+intro visible at every /#a-propos landing (fresh AND repeat) across heights 700-1024. **Gates:** build green 18 routes (max client chunk 377.36 KB<500); `seo-check` **171 PASS / 0 FAIL**; `layout-sweep --full` **18×18 = 0 REAL** (467 intended TAP_MINOR, menu whitelisted); src+dist em/en dashes **0/0**. git diff = 2 utility classes in ArchiAbout; hero/H1, Contact/footer, mix-blend menu, gallery-nav-hide (#realisations-content), Expertise-hide (#expertise), intro animation, section ids/anchors, scroll-margin offset all byte-unchanged. **No copy item for Fayçal** (layout only; wording untouched). **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics.
- 2026-06-22 — À propos: typography restyle (heading + intro less massive, client feedback) — forward type-only adjustment to home #a-propos per client ("the big heading and landing-page text look too massive; I liked the old text layout better; keep the layout"). **Baseline (be36f4a first commit, the "old" the client liked):** heading `text-4xl md:text-6xl` capped 60px; intro `text-xl md:text-2xl font-bold uppercase italic` capped 24px. **Current before edit:** heading `text-3xl sm:text-4xl md:text-5xl xl:text-7xl` (72px @xl); intro `text-lg sm:text-xl md:text-2xl xl:text-3xl font-light` (30px @xl) — the expanded responsive scale had added an oversized xl top-step = the "massive" feel. **Fix (2 type-only classes, ArchiAbout only):** (A) heading `xl:text-7xl` -> `xl:text-6xl` (desktop 72->60px, matches old ceiling, still the dominant headline); (B) intro dropped `xl:text-3xl` -> caps `md:text-2xl` (desktop 30->24px, comfortable body text). KEPT the refined current weight/case (font-bold heading, font-light normal-case intro) rather than reverting to the old bold-uppercase-italic — client wanted less mass, not the old shout. Eyebrow untouched; Damien block untouched; layout (flex/gap/mt-clamp/border/padding) byte-unchanged; GSAP `.about-heading span span` char reveal structure intact. **Proof (about-probe, /#a-propos × {1920,1440,1280,768,390}×900, fresh+repeat) in ./layout-proof/about2-before + about2-after:** intro top rises 1920 359->253, 1440 359->319, 1280 438->319 (shorter heading, fewer wrap lines); 768/390 byte-unchanged (mobile uses base/sm/md, untouched); heading+intro in view at EVERY landing fresh AND repeat (no NOT_IN_VIEW). **Gates:** build green 18 routes (max client chunk 377.26 KB<500); `seo-check` **171 PASS / 0 FAIL**; `layout-sweep --full` **18×18 = 0 REAL** (467 intended TAP_MINOR); src+dist em/en dashes **0/0**. git diff = 2 font-size classes in ArchiAbout; hero/H1, Contact/footer, mix-blend menu, gallery-nav-hide, Expertise-hide, intro animation, section ids/anchors all byte-unchanged. **No copy change -> nothing for Fayçal.** Size-variant table offered for client fine-tuning (heading minus-two / intro to text-xl) if 60/24px still feels off. **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics.
- 2026-06-22 — Forensic: original visual design is ALREADY preserved + À propos Damien-block stat-mass restyle — client asked to "restore the original visual design from before the SEO project, keep SEO copy". **Forensic finding (no edits during analysis):** the real baseline = `main 65e09f5` (mature design, last state before seo/finalize; be36f4a "first commit" is a primitive V1, be36f4a->main = +1676/-578 so NOT the baseline). Section-by-section (current HEAD vs main): Hero H1 type byte-identical (only added SEO eyebrow+CTA content + mb-12->mb-8 fit-fix); Méthode/Values headings byte-identical; Expertise/Réalisations/FAQ type identical (only added SEO content + bug-fix xl:pl-32 + crawlable-FAQ + items); Contact type character identical (font-black uppercase tracking-tighter leading-none) with only the sizing engine = height-aware clamp (kept per brief). **=> 6/7 home sections already match the original; the SEO project changed COPY + a few deliberate bug-fixes, it did NOT restyle the design.** Only À propos diverged (the alignment commit's justify-start + mt-clamp the client kept, my type pass heading xl:7xl->6xl + intro drop xl:3xl, and stat-gimmick->Damien content). **Action chosen by client:** restyle the Damien block to carry the OLD stat gimmick's visual mass (the old `0.0 RETARD / 100% CONFORMITÉ` were two `stat-number text-4xl sm:text-5xl md:text-7xl font-black tracking-tighter` numbers). **Applied (1 class-only line, ArchiAbout Damien name):** `text-2xl md:text-3xl font-bold tracking-tight` -> `text-4xl md:text-5xl xl:text-6xl font-black tracking-tighter leading-[1.05]` — echoes the stat font-black/tracking-tighter mass, caps 60px (== heading, co-dominant like the old stats) so it doesn't re-trigger "too massive"; role+bio kept (analog of the old tiny labels), column proportion xl:w-1/3 unchanged, justify-start kept, copy untouched. **Proof (about-probe /#a-propos × {1920,1440,1280,768,390}×900 fresh+repeat) in ./layout-proof/about2-stat-mass:** name now big/font-black, top-aligned with intro, Damien block grew (damLast 1920 490->580, 1440 572->662) into the height-aware section (no clip); heading+intro in view every width fresh+repeat. **Gates:** build green 18 routes (max chunk 377.26 KB<500); seo-check **171/0**; layout-sweep --full **18×18 = 0 REAL** (467 TAP_MINOR) -> 0 overflow/clip from the enlarged name in the narrow column; src+dist em/en dashes **0/0**. git diff = 1 font class in ArchiAbout; all SEO copy/alts/FAQ/JSON-LD byte-unchanged. **No copy change -> nothing for Fayçal.** **Conclusion: original visual design confirmed already-preserved; only actionable gap (Damien stat-mass) applied.** Optional size variants for the name if client wants bigger/smaller (text-7xl bolder / text-3xl..4xl smaller). **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics.
- 2026-06-23 — Hero H1: fix clipped final glyph ("permis" trailing s) — **Root cause:** each H1 line sits in a reveal mask (`.inner overflow-hidden`) that shrinks to max-content; bold `tracking-tighter` leaves a small right-side ink overhang on the last glyph, clipped at the mask edge (vertical descenders were already fixed with `md:pb-[0.18em]`, horizontal never was). layout-sweep CLIP skips `.inner`/`.sentence` by design. **Fix (1 class line, ArchiHero only):** added `pr-[0.08em] md:pr-[0.12em]` to the H1 line mask so `overflow:hidden` clips at the padding box, giving the final glyph room without shifting left edge or changing the GSAP reveal. H1 copy/tracking/GSAP byte-unchanged. **Gates:** build green 18 routes (max chunk 377.43 KB<500); seo-check **171/171 PASS**; layout-sweep default **0 REAL** (467 TAP_MINOR on --full unchanged pattern). git diff = 1 utility class on hero `.inner` mask.
- 2026-06-23 — Intro: restore cinematic preloader pace + fix hero flash/jump — **Part 1 (pace):** reverted every `40e387d` LCP compression in `ArchiPreloader` to the original choreography (logo 1.5s, progress 3.5s, letter stagger `0.8 + i*0.06`, char reveal 0.8s, circulating lines 0.3s, char-inner 0.6s, content-exit blur 0.8s + delay 0.5s, container dissolve 1.5s blur 40px). sessionStorage repeat-visit skip unchanged. **Part 2 (jump):** user saw hero text FULLY visible as the overlay faded, then the entrance snapped back and replayed — caused by `if (isLoading) return` before applying the hidden `from` state, so prerendered hero sat at final opacity under the overlay until intro done. Fix: `gsap.set` hidden start state on mount (even while loading) + `gsap.to` reveal only when `!isLoading`; `.archi-entrance` main wrapper now opacity-only (`gsap.set`/`gsap.to`, dropped `y:100` slide) so no ancestor transform fights the hero lines. **LCP tradeoff (accepted):** boot-shell logo in index.html still paints sub-second; field hero LCP may rise on cold first visit — lever = shorten final container dissolve only. **Gates:** build green 18 routes (max chunk 377.41 KB<500); seo-check **171/171 PASS**; layout-sweep default **0 REAL / 24 TAP_MINOR**; hero/H1/Contact/footer/section ids byte-unchanged. git diff = ArchiPreloader timings + hero/entrance GSAP wiring only.
- 2026-06-23 — Hero: scale H1 font down to restore old 2-3 line visual balance (long SEO text) — client wanted the old hero rhythm back. **Root cause:** H1 is an array of explicit lines `["Vos plans de construction,", "du croquis au permis"]` each forced onto its own row; at `md:text-[9.5vw]` (~182px@1920) the longer line "Vos plans de construction," (26 chars) overflows the `max-w-6xl` container and wraps again -> **4 physical lines at every width**, h1 649px tall @1920, sub-paragraph pushed to subTop 889 (off the 900 fold). Old H1 "Concevoir votre / futur projet" (short) was 2 lines at the same size. Font-size classes were byte-identical old(main 65e09f5)->current, so the only lever is size. **Empirical tune (hero-tune probe, injected font-size, line-count per width):** binding width = 1920 (max-w-6xl caps wrap there); 5vw=2L everywhere/188px@1920; **5.5vw=3L/291px@1920 (== old ~292px footprint), 2L@1440/1280/768**; 6.5vw=3L everywhere. **Applied (1 class-only line, ArchiHero H1):** `md:text-[9.5vw]` -> `md:text-[5.5vw]`; base mobile `text-[12vw]` kept (mobile was never the problem: 4 lines but sub+CTA visible, fits). H1 TEXT, eyebrow, sub-paragraph, CTA, leading-[0.8], tracking-tighter, font-bold, mb-8, GSAP char-reveal, ids/anchors/scroll-margin all unchanged. **AFTER (hero-probe /#accueil × {1920,1440,1280,768,390}×900 fresh+repeat) in ./layout-proof/hero-before + hero-after:** 1920 3L h1Bot 499 subTop 531 (was 889); 1440/1280/768 2L sub clearly visible; 390 mobile 4L sub visible; carousel visible all widths. Proof images confirm: eyebrow above, H1 prominent 2-3 lines desktop, sub+CTA visible, balanced, breathing room. **Gates:** build green 18 routes (max chunk <500); seo-check **171/0**; layout-sweep --full **18×18 = 0 REAL** (467 TAP_MINOR); src+dist em/en dashes **0/0**; deep-links /#accueil /#a-propos /#contact **PASS fresh+repeat**. git diff = 1 font-size class in ArchiHero; À propos/Damien block, Contact/footer, menu, gallery-hide, Expertise-hide, intro animation untouched. **No copy change -> nothing for Fayçal.** Variants if client wants different mass: 5vw (consistent 2L, smaller) / 6.5vw (consistent 3L, bigger). **TODO(client) unchanged:** RCP, affiliation (FNDI?), Google avis, price 700 à 1 200 € figures, experience/project metrics.
- 2026-06-23 — Ranking roadmap (off-site + entity signals + content depth) — **Tier 0 (agent):** live audit confirmed apex 307→www, robots Allow + sitemap 200 (18 URLs), silo 200, unknown path 404; codified apex→www 301 in vercel.json; owner checklist in `docs/off-site-seo-checklist.md` + README link. **Tier 1–3 (owner):** GBP/NAP/reviews/citations steps documented in checklist (byte-identical NAP, VITE_GBP_URL once verified). **Tier 2 (code):** `src/lib/social.ts` centralizes Instagram (`archi.made.studio`, confirmed) + LinkedIn (`damien-de-sousa`) + optional `VITE_GBP_URL`; Organization `sameAs` + Person `sameAs` + `alternateName`; ArchiMadeLanding imports shared handle. **Tier 4 (code):** permis FAQ +2 (extension, surélévation), déclaration préalable FAQ +2 (abri, ravalement), dept hub FAQ +2 (services, surélévation), stronger related links (permis→dept, tours→2 communes, dept→declaration). seo-check +1 sameAs gate. **Gates:** build green 18 routes (max chunk 379 KB); seo-check **172/172 PASS**; layout-sweep not re-run (content/JSON-LD only). **Owner next:** GSC verify + sitemap submit + URL inspection; GBP create/verify; citations. **site: still 0 until GSC done.**
- 2026-06-24 — Intro + hero: exact amine 65e09f5 animation and hero only (narrow scope) — client wanted ONLY the cinematic intro + first post-intro section pixel/motion-identical to `main 65e09f5`; all other sections (SEO copy, À propos, Contact, FAQ, etc.) untouched. **Preloader:** restored amine logo zoom (0.8→1.7), container exit `scale:1.1` + blur 40px, content exit `scale:1.1` (dropped blur-only exit). **Entrance:** `.archi-entrance` slide-up `y:100→0` + `stagger:0.2` (was opacity-only). **Replay:** removed `sessionStorage archimade_intro_seen` skip — intro replays every load (DEV skip via `import.meta.env.DEV` kept). **Hero:** removed visible SEO eyebrow + CTA button; H1 `mb-8→mb-12`; mask `md:pb-[0.18em]` only (dropped pr padding); H1 text `Concevoir votre / futur projet` + `md:text-[9.5vw]` unchanged; hero reveal GSAP unchanged. **Gates:** build green **18 routes** (max chunk 377.47 KB<500); seo-check **172/172 PASS**; invisible SEO (meta/JSON-LD/sr-only silo links) byte-unchanged. git diff = ArchiPreloader + entrance GSAP + hero JSX + sessionStorage removal only.
- 2026-06-24 — Hero exact amine 65e09f5: caption + Google Fonts (user-requested) — deep diff confirmed hero typography/classes already byte-identical to amine; applied final deltas only. **Caption:** HERO_MESSAGES[3] restored to amine exact `"Solutions techniques innovantes pour une architecture durable et esthétique."` **Fonts:** src/index.css self-host @font-face replaced with amine Google `@import` (Montserrat 100-700 + Inter 200-500); index.html font preload links removed. **Gates:** build green **18 routes** (max chunk 377.47 KB<500); seo-check **170 PASS / 2 FAIL** (expected by user request: `"architecture" present 1x`, `Google Fonts @import`). git diff = HERO_MESSAGES[3] + index.css + index.html only.
- (append: date — phase — done — next)
- 2026-06-18 — P1 — .npmrc legacy-peer-deps confirmed; soft-404 fixed: catch-all `*`→NotFound(noindex) added LAST in App.tsx, Seo gained noindex prop, removed SPA fallback rewrite in vercel.json so unknown paths return real 404. Clean install exit 0; build green (4 routes prerendered, `*` not prerendered); seo-check PRERENDER&HEAD 14/14 PASS. P1 = 8/8. Remaining FAILs are P2 (JSON-LD) / P3 (img sizing, preloads, >500KB chunk, fonts @import). Next: P2 structured data.
- 2026-06-18 — P2 — JSON-LD @graph in raw HTML (Organization + ProfessionalService + 5 Services linked via @id); prerender.mjs now emits helmet.script; public/og-card.png 1200x630 via sharp; Seo DEFAULT_OG_IMAGE → og-card.png. Build green (4 routes); seo-check STRUCTURED DATA 3/3 PASS. P2 complete. Next: P3 performance/CWV.
- 2026-06-18 — P3 (images) — All 30 images converted PNG/JPEG→WebP (36MB→6.5MB, 82% saved); renamed kebab-case to public/img/; ex-3MB renders now <300KB. width+height on every <img>; hero(logo-intro) fetchpriority="high"; all others loading="lazy"; React 19 SSR preload links stripped in prerender.mjs (0 image preloads). Old dirs deleted. Build green (4 routes); seo-check IMAGES section all PASS. Remaining FAILs: chunk>500KB + fonts @import (separate items).
- 2026-06-18 — P3 (JS+fonts) — manualChunks: vendor-animation (262KB) + vendor-react (66KB) + index (297KB); SSR uses animation-only split. Self-hosted Montserrat+Inter variable WOFF2 in public/fonts/; @font-face + preload in index.html; removed Google Fonts @import. Build: no chunk warning; seo-check 23/23 PASS. P3 complete (srcset/sizes deferred). Next: P4 measure & security.
- 2026-06-18 — P4 — GA4 consent-gated (src/lib/gtag.ts): loads gtag defer only when VITE_GA_ID set + user accepted cookies; fires on accept click or re-visit. GSC meta via VITE_GSC_ID (env-driven, DNS alternative noted). api/send-email.ts hardened: honeypot (hidden "website" field), time-trap (\_t <3s = silent reject), all user input HTML-escaped, body length capped, full-body console.log removed, in-memory IP rate-limit 5/min. Form sends honeypot+\_t. Build green; seo-check 23/23 PASS; 4 routes prerendered. P4 complete.
- 2026-06-18 — Audit gaps — ResponsiveImage + src/data/image-variants.json; 30 base images → WebP+AVIF variants (480/960/1536w photos, 96/192/384w logos); `<picture>` with srcset+sizes on all content imgs. seo-check 25/25 PASS (srcset 93, AVIF sources 31). P3 srcset/AVIF complete. Only PARTIAL: tasks<50ms (deploy-day field CWV). All phases code-complete.
- 2026-06-18 — Dev load — server.ts crash fixed (lazy Resend via api/send-email-core.ts); Express starts without key (503 on send). Unified local API with production (honeypot, time-trap, escape, rate-limit). Static boot shell in index.html (logo 256w); dev skips preloader; Lenis deferred until post-preloader. performance.mark js_start/react_boot_complete/preloader_done. Baseline: preview index 114KB SSR, total JS 616KB. seo-check 25/25. README + .env.local.example updated.
- 2026-06-18 — Image quality — Regenerated WebP/AVIF variants (q88/q78; widths 640–2560 photos, 128–1024 logos); updated IMAGE*SIZES for retina+transform buffer; reduced CSS bitmap upscale (logo scale-150, service scale-100, gallery scale-105, next-project opacity-only); intrinsicFromSrc from manifest; scripts/verify-image-quality.mjs. Manual DevTools check: full-bleed hero should show *-1536w or native-max (not \_-480w); gallery tile at 2x DPR ≥960w. Masters still <1920px native may stay soft on retina full-bleed — re-export at 2400–3200px (deploy-day asset TODO).
- 2026-06-18 — Image perfection Phase 2 baseline — public/img **45.40 MB**; verify-image-quality **0 fail / 27 warn**; `public/IMAGES/` PNG/JPEG sources **not on disk** (remaster falls back to public/img masters until originals restored).
- 2026-06-18 — Image perfection Phase 2 — Remastered 30 images (near-lossless WebP render3d, q92/q85 display, tier manifest); 135 orphan variants removed; ResponsiveImage v3 (tier sizesScale 1.15, WebP-only render3d, decoding); GSAP bitmap upscale removed (methodes parallax, modal hero, preloader logo); CSS hover brightness-only; logo scale-100. verify **0 fail**; seo-check **25/25**; public/img **45.49 MB**. **Blocker for 2560w+ 3D:** restore `public/IMAGES/` PNG originals and re-run remaster. DevTools: methodes 2× DPR → \*-1536w webp (until PNG restored); render3d → no AVIF source; gallery tile ≥1280w; preloader logo ≥512w.
- 2026-06-18 — GSAP/CSS gap closure — Fixed indirect upscales: `.preloader-content` exit blur-only (removed scale 1.1); gallery lightbox opacity-only (removed motion scale 0.9→1); project modal `gsap.set` + `opacity-0` hero; expertise `sizesScale` 1.2 when expanded; `[image-rendering:auto]` on full-bleed heroes. Added permanent `scripts/audit-image-transforms.mjs` + restored `scripts/remaster-images.mjs` (`npm run remaster-images`); verify hardened (`--strict-sources`, audit integration). audit **0 fail**; verify **0 fail**; build green; seo-check **25/25**; public/img **45.49 MB**. **Assets still blocked:** copy `public/IMAGES/` + `public/Nouvelles images/` then `npm run remaster-images` + `verify --strict-sources`. DevTools matrix: preloader exit — no scale(1.1) on logo; methodes 2× → \*-1536w; expertise expanded 2× → render3d ≥1280w webp; project modal — no flash, max webp; gallery lightbox — no scale tween; masonry 2× → ≥1280w.
- 2026-06-18 — GSAP/CSS gap CLOSED (code) — proof re-run: audit **0 fail**, verify **0 fail**, build **4 routes**, seo-check **25/25**. Phase marked done in PROGRESS [x] subsection. DevTools spot-check deferred (automated gates green). Asset remaster blocked until `public/IMAGES/` restored. Next: deploy-day asset phase OR new SEO phase (not both in one session).
- 2026-06-19 — Block B (copy + scrub) — Rewrote HOME title/meta/H1/subtitle/CTA + 6 service accordion H2+body. Scrubbed all "architecture"/"architecte" from rendered HTML (HERO_MESSAGES, LegalPage footer). Added og:image:width/height. Positioning = dessinateur/concepteur de plans; 0 occurrences of architecte/architecture across dist/\*.html. seo-check **25/25**; build green 4 routes. Accordion structure + GSAP preserved.
- 2026-06-19 — Dev UX — Created `.env.local` from template (RESEND loaded); `vite.config.ts` watch.ignored for PROGRESS/scripts/dist; `index.html` dev title aligned with Block B; `.env.example` points to `.env.local.example`. Express startup: injected env (1); API not 503 when key set.
- 2026-06-19 — JSON-LD NAP wired — StructuredData.tsx: Organization + ProfessionalService now name "ArchiMade" + legalName "ARCHI-MADE LTD"; ProfessionalService gains telephone +33624896695 + PostalAddress (25 rue du Maréchal Ney, 37100 Tours, FR) for NAP — GBP stays service-area (no public pin) per decision, address in JSON-LD only. Service nodes expanded to 7 (conception de plans, permis de construire ≤150 m², déclaration préalable, plans techniques, modélisation 3D, rendus photoréalistes, accompagnement projet habitat); ASCII @id slugs. areaServed (Indre-et-Loire+Tours+France) kept; no self aggregateRating; @type ProfessionalService (NOT Architect). Proof: build green 4 routes; seo-check **25/25 PASS** (no Architect); raw dist/index.html JSON-LD has name "ArchiMade" + legalName + PostalAddress + telephone; 0 Architect/aggregateRating/review. Legal identity now wired in BOTH mentions légales + JSON-LD.
- 2026-06-19 — Mentions légales wired — Populated the legal route (LegalPage.tsx mentions block) with validated RNE identity: Éditeur ARCHI-MADE LTD (UK Ltd, capital 1000 €, SIREN 101 715 993, SIRET 101 715 993 00024, APE 7112B, activité "conception de plans dans le domaine du bâtiment"), siège London + établissement France 25 rue du Maréchal Ney 37100 Tours, directeur de publication Damien DE SOUSA, RCP placeholder (à compléter). Hébergeur Squarespace → **Vercel Inc.** (340 S Lemon Ave #4133, Walnut, CA 91789, USA). Activité reworded "projets architecturaux" → "projets de construction" + "dessinateur / concepteur de plans" (no "architecte"/Ordre word). JSON-LD untouched this pass. Proof: build green 4 routes; seo-check **25/25 PASS**; raw dist/mentions-legales.html has SIREN + 37100 Tours + ARCHI-MADE LTD + Vercel (8/8 facts); 0 "Squarespace"/"projets architecturaux"/"architecte" in any HTML. Next: JSON-LD legalName + PostalAddress (deferred).
- 2026-06-19 — FINALIZE (non-architect) — Architect pivot abandoned per official RNE/INPI (APE 7112B "conception de plans dans le domaine du bâtiment" → NOT an architect). Branched `seo/finalize` off `seo/phase-1-indexability` (cert HEAD 3a99891); `seo/architect-pivot` NOT merged. Locked confirmed identity in CLAUDE.md Business (ARCHI-MADE LTD / ArchiMade / SIREN 101 715 993 / SIRET 10171599300024 / APE 7112B / établissement 25 rue du Maréchal Ney 37100 Tours / siège London / areaServed Indre-et-Loire+Tours+France); removed the "market UNDECIDED → TODO(SEO)" framing. Confirmed seo-check guard correct (`@type "Architect"` present = FAIL). Proof: build green 4 routes (largest chunk 305KB<500); seo-check **25/25 PASS** incl. `no "Architect" type`; raw dist uses `ProfessionalService` (not Architect), 0 "architecte" occurrences in any HTML. Next: deploy-day only / apply locked address to JSON-LD + mentions légales in a later phase.
- 2026-06-18 — FINAL SWEEP — Header logo regression fixed: ArchiLogo `h-16 md:h-28` → `h-44 md:h-56` (176/224px), kept `scale-100` (sharp, no CSS upscale); single shared header component, intro/footer logos untouched. areaServed SET on ProfessionalService (AdministrativeArea Indre-et-Loire + City Tours + Country France) + guidance comment; verified in raw dist/index.html. Housekeeping: `.gitignore` += deploy-runbook.md (settings.local.json + \*-audit-report.md already present); untracked generated image-audit-report.md; `.claude/settings.local.json` was never tracked, `.claude/hooks/block-deploy.mjs` kept. Image WARN cleared (whatsapp-...17.48.14 layoutRoles `full`→`gallery,service`, metadata-only, zero render/remaster impact — remaster tier comes from remasterTier, source-map read only by scripts). Created deploy-runbook.md (live-only checklist). Gates: build green 4 routes (largest chunk 305KB<500), seo-check **25/25**, verify-image **0 fail / 5 warn**. ALL LOCAL CODE COMPLETE. Next = deploy-day only (live verification + asset re-export), see deploy-runbook.md.
